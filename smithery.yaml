name: lmcprotocol
displayName: LMCProtocol
description: Sub-Second Local Business Telemetry & Flaw Diagnostics in ~210 Tokens for AI Agents (OBDS-2026).
version: 1.0.0
homepage: https://lmcprotocol.com
repository: https://github.com/quitegoodproject/lmcprotocol
license: MIT
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: LMCProtocol API Key (optional for free tier)
  commandFunction:
    name: npx
    args:
      - -y
      - "@quitegoodproject/lmcprotocol"
